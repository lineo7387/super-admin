---
'@super-admin-org/core': minor
'@super-admin-org/ui': patch
'create-super-admin': minor
---

Add immutable module-manifest mounting, composition, and duplicate-safe registry helpers. Generate an executable ESLint/Vitest quality baseline by default with an explicit `--minimal` opt-out, typed layout/auth registries, neutral fallbacks, and capability-aware AI context. Expose Vue as a peer runtime of the shared UI package.
