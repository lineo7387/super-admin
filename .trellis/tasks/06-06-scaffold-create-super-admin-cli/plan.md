# Scaffold create-super-admin CLI MVP Plan

- [x] Start the child task with `task.py start`.
- [x] Load Trellis implementation context and applicable specs.
- [x] Add failing CLI/generator tests for argument normalization, invalid input, and generated default/multi-theme/i18n outputs.
- [x] Add `packages/cli` with a `create-super-admin` bin and typed generator modules.
- [x] Generate a single-app Vite starter from the admin source model with required transforms and exclusions.
- [x] Reuse `pnpm validate:starter` against generated outputs.
- [x] Run `pnpm test`, `pnpm typecheck`, `pnpm lint`, `pnpm build`, and starter validation.
