# create-super-admin

Create a frontend-first Super Admin starter project.

```bash
npm create super-admin@next my-admin
```

The generated project consumes published `@super-admin-org/*` package artifacts and runs with mock data by default.
