# @super-admin-org/theme

Theme runtime helpers for applying Super Admin design profiles and resolving profile color modes.

Install this package together with one or more `@super-admin-org/theme-*` profile packages.
